name: None

on:
  workflow_dispatch:

env:
  MAKEFLAGS: -j4

jobs:
  build-r:
    strategy:
      matrix:
        os: [macos-15]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Look first
        run: |
          whereis pdflatex
      - name: Update Homebrew
        run: |
          brew update
      - name: Install LaTeX
        run: |
          brew install basictex
          eval "$(/usr/libexec/path_helper)"
      - name: Look second
        run: |
          whereis pdflatex
